apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "voluba-prod.fullname" . }}-test-connection"
  labels:
    {{- include "voluba-prod.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "voluba-prod.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
