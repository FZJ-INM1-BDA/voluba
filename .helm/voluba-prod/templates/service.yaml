apiVersion: v1
kind: Service
metadata:
  name: {{ include "voluba-prod.fullname" . }}
  labels:
    {{- include "voluba-prod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "voluba-prod.selectorLabels" . | nindent 4 }}
