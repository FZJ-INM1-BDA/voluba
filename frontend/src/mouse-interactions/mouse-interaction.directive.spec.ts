import { MouseInteractionDirective } from './mouse-interaction.directive';

describe('MouseInteractionDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseInteractionDirective();
    expect(directive).toBeTruthy();
  });
});
